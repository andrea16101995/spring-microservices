spring:
  application:
    name: calculator
  cloud:
    config:
      uri: http://localhost:8888
      
  profiles:
    active:
    - eureka
    - ribbon


ribbon:
  eureka:
    enabled: true
